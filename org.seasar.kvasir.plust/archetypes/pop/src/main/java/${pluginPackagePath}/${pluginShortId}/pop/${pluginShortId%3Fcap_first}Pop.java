package ${pluginId}.${pluginShortId}.pop;

import org.seasar.kvasir.cms.pop.pop.GenericPop;

import ${pluginId}.${pluginClassName};


public class ${pluginShortId?cap_first}Pop extends GenericPop
{
    public static final String ID = ${pluginClassName}.ID + ".${pluginShortId}Pop";
}
